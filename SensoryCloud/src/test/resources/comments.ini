# top level comment

[SECTION NAME]
fullyQualifiedDomainName="fqdn"
isSecure=true
tenantID="tenant"
credential=credential # some comment
enrollmentType=sharedSecret
deviceID = deviceID
deviceName=deviceName

# other keys are ignored
otherKey=value
foo=bar
